# Lab

Continue over lab 1, 2, 3 to do the following:

1. Use mongoose instead of filesystem based storage for the todos.
2. use `zod` to validate input for POST and PATCH endpoints.
3. Add error handling middleware to handle errors gracefully. (validation error, and general server errors)
4. Add logging middleware to log incoming requests and errors, use `morgan` or similar package.